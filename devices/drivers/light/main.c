#include<stdio.h>
int main()
{
   char light_focus_length_name,light_source_name;
   int light_id,light_rays_length;
   if(light_focus_length_name=="l")
   {
      printf("light falls on the earth by sun \n");
   }
   else
   {
     printf("light not recieved because sun is not came and also holiday for sun \n");
   }

}
